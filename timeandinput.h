#pragma once
#include <time.h>
#include <stdio.h>
#include <SDL.h>
void delay(int);
void inputdec(SDL_Event ev, Uint8* tec, int** L, int** I, int* p, int* t, int* m, int* q, int* d);
void mandarparadisplay(int* P, int* M, int* D, int* O);